[build]
  command = "node scripts/generate-env.js && npm run build"
  publish = "dist/buzzerGame/browser"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NG_BUILD_CONFIGURATION = "production"
